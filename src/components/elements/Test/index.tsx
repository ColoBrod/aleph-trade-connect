import React from 'react';

interface Props {
  name: string;
  age: number;
}

const TestComponent = (props: Props) => {
  const { name } = props;
  return (
    <div>
      
    </div>
  );
}
 
export default TestComponent;